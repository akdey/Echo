# Redis Configurations (Optional - falls back to in-memory if empty/unavailable)
REDIS_HOST=localhost
REDIS_PORT=6379

# Ollama Endpoint Configurations (Optional - falls back to local heuristics if empty/unavailable)
OLLAMA_URL=http://localhost:11434
